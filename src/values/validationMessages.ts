const fieldIsRequired = 'This field cannot be empty';

const invalidEmail = 'Invalid email';

const shortPassword = 'Password must contain at least 6 characters';

const passwordsDoNotMatch = 'Passwords do not match';

export { fieldIsRequired, invalidEmail, shortPassword, passwordsDoNotMatch };
