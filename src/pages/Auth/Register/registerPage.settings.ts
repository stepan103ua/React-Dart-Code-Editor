const welcomeSubtitle = 'Sign up to continue';

const usernameHint = 'Username';

const emailHint = 'Email';

const passwordHint = 'Password';

const confirmPasswordHint = 'Confirm password';

const signUpButtonText = 'Sign up';

const backToLoginButtonText = 'Back to login';

const spacing = 20;

export {
  welcomeSubtitle,
  usernameHint,
  emailHint,
  passwordHint,
  confirmPasswordHint,
  signUpButtonText,
  backToLoginButtonText,
  spacing
};
