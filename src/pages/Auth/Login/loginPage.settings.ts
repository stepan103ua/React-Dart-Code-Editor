const welcomeSubtitle = 'Sign in to continue';

const emailHint = 'Email';

const passwordHint = 'Password';

const signInButtonText = 'Sign in';

const createAccountButtonText = 'Create account';

const continueAsGuestButtonText = 'Continue as guest';

const spacing = 20;

export {
  welcomeSubtitle,
  emailHint,
  passwordHint,
  signInButtonText,
  createAccountButtonText,
  continueAsGuestButtonText,
  spacing
};
